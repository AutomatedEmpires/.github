# Contributing

1. File a `[build]` issue using the Build Task template.
2. Branch, build, and open a PR that links the issue.
3. Keep CI green — typecheck, lint, guardrails, build, test.
4. Follow the golden path (see org AGENTS.md). One icon system. Expand/contract migrations.
5. Use agent assists from PR comments where useful.